#ifndef {{ device_name_snake | upper }}_RPC_H
#define {{ device_name_snake | upper }}_RPC_H

#include <stdint.h>
#include <stdbool.h>
#include "can_frame.h"

#define RPC_GET_ID {{ rpc_get_id }}
#define RPC_ANS_ID {{ rpc_ans_id }}

/* RPC Call structure */
typedef struct {
    uint8_t service_id;
    uint8_t method_id;
    uint8_t result[6];
}RpcCall;

/* RPC Response structure */
typedef struct {
    uint8_t service_id;
    uint8_t method_id;
    uint8_t result[6];
}RpcResponse;

/* Function for RPC requests handling */
bool can_handle_{{ device_name_snake }}_rpc_requests(const CanFrame *frame, CanFrame *response);

#endif // {{ device_name_snake | upper }}_RPC_H